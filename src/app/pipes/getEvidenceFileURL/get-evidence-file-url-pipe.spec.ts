import { GetEvidenceFileURLPipe } from './get-evidence-file-url-pipe';

describe('GetEvidenceFileURLPipe', () => {
  it('create an instance', () => {
    const pipe = new GetEvidenceFileURLPipe();
    expect(pipe).toBeTruthy();
  });
});
