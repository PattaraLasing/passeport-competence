import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'ch.pattaralasing.capacitordemo',
    appName: 'capacitor-demo',
    webDir: 'dist/capacitor-demo/browser',
};

export default config;